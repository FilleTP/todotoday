<%= simple_form_for [@task_list, @invitation] do |f| %>
  <%= f.input :invitee_id, collection: @profiles, label_method: :nickname, value_method: :id, label: "Select user to invite", prompt: "Choose..." %>
  <%# <%= f.hidden_field :inviter_id, value: current_user.id %>
  <%# <%= f.hidden_field :task_list_id, value: @task_list.id %>
  <%= f.button :submit %>
<% end %>
