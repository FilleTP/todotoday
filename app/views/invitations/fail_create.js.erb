alert("<%= j "Failed to invite #{@invitation.invitee.nickname} to your list..try again and make sure the user is not already invited! #{link_to 'Manage invitations', profile_path(@invitation.inviter)}" %>")
